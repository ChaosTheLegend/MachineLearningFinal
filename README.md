# MachineLearningFinal
Repo for machine learning final project

To istall the project, clone it and run the command:

```$ pip install -r requirements.txt```

This should install all the requirements
